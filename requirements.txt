# Beat Mangler — Python dependencies
# Install with: pip install -r requirements.txt
#
# Note: essentia wheels are only published for Linux & macOS.
# On Windows, use WSL2 or run in Google Colab.

essentia>=2.1b6
pydub>=0.25.1
numpy>=1.24
tqdm>=4.65

# Optional — only needed for video support
moviepy>=1.0.3

# ffmpeg must be installed separately at the system level:
#   macOS   → brew install ffmpeg
#   Ubuntu  → sudo apt install ffmpeg
#   Windows → https://ffmpeg.org/download.html  (or use WSL2)
